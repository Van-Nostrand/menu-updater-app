<%- include('partials/header') %>

  <div><%= category %></div>

  <ul>
    <% allItems.forEach((item, i) => { %>
      <%- include('./partials/beeritem', { beer: item }) %>
    <% }) %>
  </ul>

<%- include('partials/footer') %>
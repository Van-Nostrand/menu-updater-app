<%- include('partials/header') %>

  <div><%= category %></div>
  <% if(created){ %>
    <div>successfully created <%= created.name %></div> 
  <% } %>
  <ul>
    <% allItems.forEach((item, i) => { %>
      <%- include('./partials/fooditem', {item, mode: "display"}) %>
    <% }) %>
  </ul>

<%- include('partials/footer') %>
<%- include('partials/header') %>

  <div><%= category %></div>

  <ul>
    <% allItems.forEach((item, i) => { %>
      <%- include('./partials/spirititem', { spirit: item }) %>
    <% }) %>
  </ul>

<%- include('partials/footer') %>
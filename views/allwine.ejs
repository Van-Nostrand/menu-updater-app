<%- include('partials/header') %>

  <div><%= category %></div>

  <ul>
    <% allItems.forEach((item, i) => { %>
      <%- include('./partials/wineitem', { wine: item }) %>
    <% }) %>
  </ul>

<%- include('partials/footer') %>
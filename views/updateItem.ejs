<%- include('partials/header') %>

  <div>Edit Item</div>
  <% if(item.itemType === "beer"){ %>
    <%- include('./partials/beeritem', {item, mode: "edit"}) %>
  <% } %>
  <% else if(item.itemType === "food") {
    <%- include('./partials/fooditem', {item, mode: "edit"}) %>
  <% } %>
  
  <div class="edit-buttons">
    <form action="<%= `/beers/edit/${item.id}` %>?_method=DELETE" method="POST">

      <button type="submit">delete me</button>
      
    </form>
  </div>

<%- include('partials/footer') %>